#write a python programme to cheak for a number at the end of a word /sentence
import re
def end_sum(string):
    text=re.compile(r".*[0-9]$")
    if text.match(string):
        return True
    else:
        return False
print(end_sum('abcdef'))
print(end_sum("abcdef8"))




